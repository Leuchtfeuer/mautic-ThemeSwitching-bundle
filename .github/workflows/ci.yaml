name: CI Workflow.

on:
  pull_request:
  workflow_dispatch:

jobs:
  ai-review:
    name: AI Code Review
    uses: Leuchtfeuer/mautic-ci-runner/.github/workflows/ai_review.yaml@d02185a0b9c93f945a2a889812def7b86a82ee5e
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
